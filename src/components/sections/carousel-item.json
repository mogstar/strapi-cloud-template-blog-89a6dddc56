{
  "collectionName": "components_sections_carousel_items",
  "info": {
    "displayName": "Carousel item",
    "icon": "calendar",
    "description": "Single card within a carousel section (event or editorial piece)"
  },
  "options": {},
  "attributes": {
    "title": {
      "type": "string",
      "required": true
    },
    "date": {
      "type": "string",
      "description": "Display string, e.g. \"29.05.25\" — not a real date field, matches Figma copy"
    },
    "location": {
      "type": "string"
    },
    "image": {
      "type": "media",
      "multiple": false,
      "required": false,
      "allowedTypes": ["images"]
    },
    "tags": {
      "type": "json",
      "description": "Array of pill labels shown over the image, e.g. [\"Events\", \"UNSTABLE\"]"
    }
  }
}
